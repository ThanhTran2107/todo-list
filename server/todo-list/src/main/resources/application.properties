# PostgreSQL datasource
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=123456
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/todo_db

# Hibernate ORM & schema
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true

# Swagger UI (OpenAPI UI)
quarkus.swagger-ui.path=/q/swagger-ui
quarkus.swagger-ui.always-include=true

# JWT Security
quarkus.smallrye-jwt.enabled=true

# JWT issuer for token verification (must match in JwtGenerator)
smallrye.jwt.sign.key.location=META-INF/resources/privateKey.pem
mp.jwt.verify.publickey.location=META-INF/resources/publicKey.pem

# CORS
quarkus.http.cors=true
quarkus.http.cors.origins=*
mp.jwt.verify.issuer=todo-app

quarkus.mailer.from=thanhcnttmcpe@gmail.com
quarkus.mailer.host=smtp.gmail.com
quarkus.mailer.port=587
quarkus.mailer.username=thanhcnttmcpe@gmail.com
quarkus.mailer.password=qldq kypn rbuy jvfq
quarkus.mailer.starttls=REQUIRED
quarkus.mailer.debug=true

quarkus.log.category."io.quarkus.mailer".level=DEBUG

quarkus.scheduler.enabled=true
quarkus.mailer.mock=false

